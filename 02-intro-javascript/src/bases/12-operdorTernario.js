// Operacion condicional ternario

const active = true;

// FormaTradicional

// let mensaje = '';

// if( !activo ) {
//     mensaje = 'activo';
// } else {
//     mensaje = 'inactivo';
// } 


// Con Operador Ternario

const message = ( active ) ? 'Active' : 'Inactive';

console.log(message);

